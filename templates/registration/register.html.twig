{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}
{% block meta_description %}
    Créez votre compte sur Exploréa et choisissez votre rôle : Hôte pour publier des annonces ou Voyageur pour réserver des hébergements et des activités. Rejoignez-nous dès aujourd'hui !
{% endblock %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/notif.css') }}">
{% endblock %}
{% block importmap %}{{ importmap('register') }}{% endblock %}
{% block header %}{% endblock %}
{% block footer %}{% endblock %}

{% block body %}
    <div class="container">
        <div class="authentif-form">
            <h2>Inscription</h2>

            {{ form_start(registrationForm) }}
            <div class="form-group">
                {{ form_label(registrationForm.email, 'Email <span class="required-symbol">*</span>', { 'label_html': true }) }}
                {{ form_widget(registrationForm.email, {'attr': {'class': 'form-control'}}) }}
                <div class="error">
                    {{ form_errors(registrationForm.email) }}
                </div>
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.password.first, 'Mot de passe<span class="required-symbol">*</span>', { 'label_html': true }) }}
                <div class="password-container">
                    {{ form_widget(registrationForm.password.first, {'attr': {'class': 'form-control', 'id': 'password'}}) }}
                    <span id="FirstPassword" class="eye-icon" onclick="showPassword('registration_form_password_first')">👁️</span>

                </div>
                <div class="error">
                    {{ form_errors(registrationForm.password.first) }}
                </div>
            </div>


            <div class="form-group">
                {{ form_label(registrationForm.password.second )}}
                <div class="password-container">
                    {{ form_widget(registrationForm.password.second, {'attr': {'class': 'form-control', 'id': 'confirmPassword'}}) }}
                    <span id="ConfirmPassword" class="eye-icon" onclick="showPassword('registration_form_password_second')">👁️</span>
                </div>
                <div class="error">
                    {{ form_errors(registrationForm.password.second) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(registrationForm.firstName, 'Prénom <span class="required-symbol">*</span>', { 'label_html': true })  }}
                {{ form_widget(registrationForm.firstName, {'attr': {'class': 'form-control'}}) }}
                <div class="error">
                    {{ form_errors(registrationForm.firstName) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(registrationForm.lastName, 'Nom <span class="required-symbol">*</span>', { 'label_html': true })  }}
                {{ form_widget(registrationForm.lastName, {'attr': {'class': 'form-control'}}) }}
                <div class="error">
                    {{ form_errors(registrationForm.lastName) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(registrationForm.gender, 'Genre <span class="required-symbol">*</span>', { 'label_html': true }) }}
                {{ form_widget(registrationForm.gender) }}
                <div class="error">
                    {{ form_errors(registrationForm.gender) }}
                </div>
            </div>

            <div class="form-group">
                {{ form_label(registrationForm.role,'Vous êtes <span class="required-symbol">*</span>', { 'label_html': true }) }}
                    {{ form_widget(registrationForm.role) }}
                <div class="error">
                    {{ form_errors(registrationForm.role) }}
                </div>
            </div>


            <div class="agree-terms-row">
                {{ form_label(registrationForm.agreeTerms) }}
                {{ form_widget(registrationForm.agreeTerms) }}
                <div class="error">
                    {{ form_errors(registrationForm.agreeTerms) }}
                </div>
            </div>

            <button type="submit" class="btn" aria-label="inscription">S'inscrire</button>
            {{ form_end(registrationForm) }}
        </div>
        <div class="illustration">
            <img class="img-register" src="{{ asset('images/register.png') }}" alt="Illustration">
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('js/showPassword.js') }}"></script>
{% endblock %}


