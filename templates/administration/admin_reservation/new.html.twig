{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/Admin/new.css') }}">
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Ajouter une nouvelle  Reservation</h1>

        {{ include('administration/admin_reservation/_form.html.twig') }}

        <a href="{{ path('admin_reservation_index') }}">Retour à la liste</a>
    </div>
{% endblock %}

