{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('styles/Admin/new.css') }}">
{% endblock %}


{% block body %}
<div class="container">
    <h1>Modifier le statut de réservation {{ status.status }}</h1>

    {{ form_start(form) }}
    {{ form_row(form.status) }}
    <button class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('app_admin_reservation_status_list') }}" class="btn btn-secondary">Retour à la liste</a>
{% endblock %}
